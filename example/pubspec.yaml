name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  simple_tooltip:

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  simple_tooltip:
    path: ../

flutter:
  uses-material-design: true
